<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_menu_home"
            android:title="@string/home_fragment_title" />
    </group>

    <item
        android:id="@+id/nav_exit"
        android:icon="@drawable/ic_exit_image"
        android:title="@string/exit_menu_string" />

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="@string/share_menu_string" />
            <item
                android:id="@+id/nav_donate"
                android:icon="@drawable/ic_menu_donate"
                android:title="@string/donate_menu_string" />
        </menu>
    </item>

</menu>
